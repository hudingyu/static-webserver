{
  "name": "static-webserver",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cp ./favicon.ico ../dist/favicon.ico && node ./app.js"

  },
  "keywords": [
    "node",
    "static",
    "server"
  ],
  "author": "hudingyu",
  "license": "ISC",
  "dependencies": {
    "http": "0.0.0",
    "yargs": "^12.0.1"
  }
}
